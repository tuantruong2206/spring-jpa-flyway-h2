CREATE TABLE drug (
                      id SERIAL NOT NULL PRIMARY KEY,
                      name VARCHAR(50) NOT NULL,
                      brand VARCHAR(100) NOT NULL,
                      company VARCHAR(100)
);
